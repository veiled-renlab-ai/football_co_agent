# Football Agent Simulation — Python dependencies
# Install in WSL Ubuntu venv:  pip install --no-build-isolation -r requirements.txt
# (gfootball needs --no-build-isolation; see DEV_PLAN.md §3 and setup_wsl_env.sh)

# === Sim environment ===
gfootball>=2.10.2
gym==0.25.2     # gfootball is incompatible with gym>=0.26 (reset/step API broke)
psutil          # gfootball's setup.py imports this during build
six             # gfootball still uses Python 2/3 compat shims
numpy<2.0       # gym 0.25 doesn't support numpy 2.0

# === LLM client (provider-agnostic, works with 火山方舟 / MiniMax / OpenAI / DeepSeek) ===
openai>=1.35.0

# === Config + utilities ===
python-dotenv>=1.0.0
pydantic>=2.5.0      # data validation for Observation / Skill payloads

# === Dev / testing ===
pytest>=7.0
